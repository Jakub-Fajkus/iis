{% extends 'admin/adminBase.html.twig' %}

{% block body %}
    <h1>Departments list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Shortcut</th>
                <th>Name</th>
                <th>Telephone</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for department in departments %}
            <tr>
                <td><a href="{{ path('admin_department_show', { 'id': department.id }) }}">{{ department.id }}</a></td>
                <td>{{ department.shortcut }}</td>
                <td>{{ department.name }}</td>
                <td>{{ department.telephone }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_department_show', { 'id': department.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_department_edit', { 'id': department.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_department_new') }}">Create a new department</a>
        </li>
    </ul>
{% endblock %}
