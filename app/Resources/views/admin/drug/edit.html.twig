{% extends 'admin/adminBase.html.twig' %}

{% block body %}
    <h1>Drug edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Upravit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('admin_drug_index') }}">Zpět na výpis</a>
        </li>
    </ul>
{% endblock %}
