{% extends 'admin/adminBase.html.twig' %}

{% block body %}
    <h1>Employment creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_doctor_show', {'id': doctorId}) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
