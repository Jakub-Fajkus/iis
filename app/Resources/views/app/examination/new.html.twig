{% extends 'app/appBase.html.twig' %}

{% block body %}
    <h1>Examination creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('app_examination_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
