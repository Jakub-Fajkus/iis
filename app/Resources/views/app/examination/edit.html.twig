{% extends 'app/appBase.html.twig' %}

{% block body %}
    <h1>Examination edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Upravit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
        <a href="{{ path('app_examination_index') }}">Zpět na výpis</a>
        </li>
    </ul>
{% endblock %}
