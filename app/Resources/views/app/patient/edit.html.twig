{% extends 'app/appBase.html.twig' %}

{% block body %}
	<div class="container">

		<h1>Pacient - úpravy</h1>
		{{ form_start(edit_form, {'attr' : {'class': 'fm'}}) }}


			<div class="fm__wrapper">
				{{ form_row(edit_form.name, { 'label': 'Jméno' }) }}
				{{ form_row(edit_form.surname, { 'label': 'Příjmení' }) }}
				{{ form_row(edit_form.street, { 'label': 'Ulice' }) }}
				{{ form_row(edit_form.houseNumber, { 'label': 'Č.p.' }) }}
				{{ form_row(edit_form.personalIdentificationNumber, { 'label': 'Rodné číslo' }) }}
				{{ form_row(edit_form.city, { 'label': 'Město' }) }}
				{{ form_row(edit_form.zip, { 'label': 'Psč' }) }}
				{{ form_row(edit_form.city, { 'label': 'Město' }) }}
				{{ form_row(edit_form.medicalIdentificationNumber, { 'label': 'Číslo pojišťence' }) }}
				{{ form_row(edit_form.insuranceCompanyId, { 'label': 'Číslo pojišťovny' }) }}
				{{ form_row(edit_form.isWoman, { 'label': 'Žena' }) }}

				<div class="fm__btn">
					<input type="submit" value="Upravit" class="btn" />
				</div>
			</div>

		{{ form_end(edit_form) }}

		<a href="{{ path('app_patient_index') }}" class="btn btn--back">Zpět na výpis</a>
	</div>

{% endblock %}
