{% extends 'app/appBase.html.twig' %}

{% block body %}
	<div class="container">
		{#TODO: PRIDAT NEJAKE VYHLEDAVANI? leku muze byt dost...#}
		<h1>Předepsání léku</h1>
		{{ form_start(form, {'attr' : {'class': 'fm'}}) }}
			{{ form_widget(form, {'attr' : {'class': 'fm__wrapper'}}) }}
			<input type="submit" value="Vytvořit" />
		{{ form_end(form) }}

		<a href="{{ path('app_prescription_index') }}"class="btn btn--back">Zpět na výpis</a>
	</div>
{% endblock %}
