{% extends 'app/appBase.html.twig' %}

{% block body %}
	<div class="container">

		<h1>Prescription edit</h1>

		{{ form_start(edit_form, {'attr' : {'class': 'fm'}}) }}
			{{ form_widget(edit_form, {'attr' : {'class': 'fm__wrapper'}}) }}
			<input type="submit" value="Upravit" class="btn"/>
		{{ form_end(edit_form) }}

		<a href="{{ path('app_prescription_index') }}"class="btn btn--back">Zpět na výpis</a>
	</div>
{% endblock %}
